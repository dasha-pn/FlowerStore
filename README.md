# FlowerStore

## Important
In our task on learn you saud that we need to create branch "flower-bucket" 
and merge them into the main branch with Pull Request, but we have done these 
class during our lesson, so I added it with the rest parts we have done during 
the lesson and instead of these I created a branch "store", where was creating 
Store class and then merge it into the main branch with Pull Request.
